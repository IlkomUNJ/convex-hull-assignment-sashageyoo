
[![Review Assignment Due Date](https://classroom.github.com/assets/deadline-readme-button-22041afd0340ce965d47ae6ef1cefeee28c7c493a6346c4f15d667ab976d596c.svg)](https://classroom.github.com/a/1PRAkQnI)
# Identity
Name : Sabiyan Avril Chandrakanta
NIM : 1313623020

# AI usage
Explain how you use the AI to solve the assignment. Put all ***your used queries*** in this section & what kind of AI are you using. Declare not using AI if you didn't do this. 

Dear Mr. Eka,
Before i write down all of my used queries to the AI that i use, i'm truly sorry for what i actually done to complete this assignment. Honestly, i'm using AI entirely to make this assignment done. AI that i used are Gemini 2.5 Flash & Pro and ChatGPT-5. Before i hop to AI, i have learned about Convex Hull Algorithm (The Graham Scan) on YouTube, i understand the concept but still struggling to pour it into coding implementation. So here is my queries from AI:

**Firstly, i use Gemini 2.5 Flash**
- can you tell me how to simulate convex hull algorithm using Qt creator? ; it shown the steps to set up the GUI template, set the visual/graphics implementation, and set the convex hull algorithm (the graham scan)
- can you simulate the convex hull algorithm if the input is any set of vertices drawn on canvas and spawned when the user is left clicked the canvas? also the output is the fully formed convex geometry getting drawn
- it worked, but can you add the clear button?
- thankyou, can you add thse things with your code? Implement both slow convex hull and convex hull algorithm and wrote the iteration numbers on canvas please
- can you show me the code?
- can you show me what classes should i make so the code its just not in the main.cpp? ; it shows me ConvexHullView class for the front-end view so it can draw vertices and line on the canvas window, and ConvexHullSimulator class for the back-end logic.
- can you show me the code for those classes?
- from the previous code, can you add these algorithm (Slow Hull and Fast Hull) and show the description in the top of the window that show a description text like "Algorithm: Fast Hull  Iterations: *number of iterations* Total Points (n): *number of total points* hull points (h): *number of hull points*"
repeat for the the slow hull "Algorithm: Slow Hull (Iterations: *number of iterations* Total Points (n): *number of total points* hull points (h): *number of hull points*"
add interactive buttons so the user can activate the slow hull algorithm and fast hull algorithm

**Then i use Gemini 2.5 Pro**
- _i write the code again*_ from this code, can you add a fast hull (graham scan algorithm) and a slow hull (brute force scan algorithm) and add the number of total points used, the number of total points hull, and the numbe rof iteration description in the top left corner window? make the user can choose from buttons like "slow hull" and "fast hull", when they click the buttons, the description that i tell you will appear in the top left corner
- it error :
"use of deleted function 'size_t qHash(QPointF, size_t)'"
"use of deleted function 'size_t qHash(QPointF, size_t)'"
'constexpr' call flows off the end of the function
- it still error like the previous


**I use ChatGPT-5 since it won't solve my problem**
- chatt can you see this code?  _i write the unseparated code since it will be complicated*_ from this code, can you add a fast hull (graham scan algorithm) and a slow hull (brute force scan algorithm) and add the number of total points used, the number of total points hull, and the numbe rof iteration description in the top left corner window? make the user can choose from buttons like "slow hull" and "fast hull", when they click the buttons, the description that i tell you will appear in the top left corner
- thankyou it worked, - can you explain to me the algorithm of the slow hull and fast hull?
- _i wirte some code snippets about slowhull and fasthull algorithm)*_ can you explain why this can be written like this? can you explain it detailly?
- if i want to change the color of the points to (66, 135, 245), where is the function?
- where can i change the color of the points?
- can you separate that main.cpp file into three separated classes so it more efficient (but still use the code above, just separate it)
- it still eror like : _i show the pic of my error codes since its so long*_
- #include <algorithm> !include header algortihm is not used directly anyway it can run, but it wont show the hull, its just showing the clicked points, but the hull is not showing up (the line, the shape of the hull is not showing)
- what about #include <algorithm> !include header algortihm is not used directly is is included by this error

i realized that my error is because i didn't write the algorithm since its separated so i write it and it worked and finished.






# Collaboration usage
Explain how you are ***using others*** to complete the assignment in this section. Are they simply copying, learn the ideas, learn how to adapt the AI or anything involving the process. State the people if any & Please be elaborate. Declare self tought if you solved this by yourself. 

i actually worked by myself and AI for this. But i communicate with some of my friends when i encountered some problems, especially the CMake didn't exists problem. My friend Nazwa Aiana Putri from the same class helped me to fix this issue. I also asked Khalisa Rajwa Nazira so my code has separated classes, and peek their assignment too just for my visual reference.

# Commit Rules
Use .gitignore to stripe any binaries from the resulting build before commit.  If you cloned directly from the template, you are safe. 

# Due Time
September 30th, 2025. 23: 59 WIB
